# App name
APP=testui

# Object files
OBJ=testui.rel multimenu.rel

# LIBS (user-defined) if LIBS not defined then ALL libraies will be used.
LIBS=libui libzxkbd libps2 libim2 libz80

# CRT0
# CRT0PATH=$(TOP)/libsrc/crt0/$(CRT0)
